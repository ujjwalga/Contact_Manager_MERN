const express = require("express");
const dotenv = require("dotenv").config();
const app = express();
const port = process..
app.listen(port, () => {
    console.log(`server running on ${port}`);   
})